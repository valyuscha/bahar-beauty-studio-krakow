{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd frontend && npm install --legacy-peer-deps && CI=false npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s frontend/build -l tcp://0.0.0.0:${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
